---
export const prerender = false;

import AdminLayout from '../../../../layouts/AdminLayout.astro';
import BlogPreview from '../../../../components/admin/youtube-to-blog/BlogPreview';

const { id } = Astro.params;
---

<AdminLayout title="Blog Preview">
  <BlogPreview postId={id!} client:only="react" />
</AdminLayout>
