// Placeholder - full implementation by blog-ui teammate
export default function BlogPostEditor({ postId }: { postId: string }) {
  return (
    <div className="p-8 text-center text-slate-500">
      <p>Blog Post Editor for: {postId}</p>
      <p className="text-sm mt-2">Loading editor...</p>
    </div>
  );
}
